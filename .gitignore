# ═══════════════════════════════════════════════════════════════════
# WHITELIST-BASED APPROACH
# See .github/commit-whitelist.md for the definitive list of allowed files
# ═══════════════════════════════════════════════════════════════════
#
# Philosophy: This is a PUBLIC repository
# - Production code and public docs: ALLOWED
# - Internal dev files, scripts, logs: BLOCKED
#
# ALLOWED directories:
#   cmd/, internal/, frontend/, installer/, .github/
# ALLOWED config files:
#   go.mod, go.sum, Makefile, wails.json, .gitignore
# ALLOWED docs:
#   README.md, LICENSE, CLI_GUIDE.md, ARCHITECTURE.md, CONTRIBUTING.md,
#   TESTING.md, RELEASE_NOTES.md, FEATURE_SUMMARY.md
#
# Everything else is BLOCKED by default
# ═══════════════════════════════════════════════════════════════════

# Binaries (in project root, not in bin/)
/rescale-int
/rescale-int-*
*.dll
*.so
*.dylib

# Test binaries
*.test
*.out

# Go workspace file
go.work

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Build artifacts (binaries distributed via GitHub Releases)
bin/
dist/
build/

# Node.js dependencies (install with npm install)
node_modules/
frontend/node_modules/

# Test data (user-specific)
*.csv
*.state
!testdata/**/*.csv

# Old reference files (not for GitHub)
old-reference/

# Development files (not for GitHub)
CLAUDE.md
.claude/

# Release automation scripts (local only, not for GitHub)
scripts/

# Internal development documentation (not for public GitHub)
FEATURE_SUMMARY.md
TODO_AND_PROJECT_STATUS.md
DOCUMENTATION_SUMMARY.md

# Logs
*.log

# Local config files with sensitive data
config.local.csv
.env
.api_key

# Internal checkpoint files
CRITICAL_BUG_CHECKPOINT.md

# Token files (API keys)
rescale_token*.txt

# This release checklist file (internal use only)
GITHUB_READY.md

# Release tarballs (distributed via GitHub Releases)
*.tar.gz

# Internal investigation/checkpoint files
BINARY_SIZE_INVESTIGATION.md
CHECKPOINT_SESSION.md
# Session checkpoint files
CHECKPOINT_*.md
# Root binary (wrong location)
rescale-int
CONTINUATION_PLAN*.md

# Local Claude development files (never commit)
scripts/CLAUDE_BUILD_INSTRUCTIONS.md
**/CLAUDE_BUILD*.md

# Terminal session logs (may contain secrets)
Terminal_*.txt
*_transcript*.txt
*_session*.txt
*_log*.txt

# Any file that might contain credentials
*token*
*secret*
*credential*
*password*
*.key
!*.tsx
!*.ts

# Claude Code artifacts that may contain sensitive data
*.compact
conversation_*.json

# Archived/internal code (not for GitHub)
_archive_*/
*_archive/

# Internal security audits (not for GitHub)
SECURITY_AUDIT*.md

# Block internal docs/ folder (internal documentation)
docs/

# Block testdata/ folder (test fixtures - internal only)
testdata/

# Catch-all: Block any .md or .txt NOT in whitelist
# (Whitelist: README.md, LICENSE, CLI_GUIDE.md, ARCHITECTURE.md,
#  CONTRIBUTING.md, TESTING.md, RELEASE_NOTES.md, FEATURE_SUMMARY.md)
# All other .md/.txt files are internal and should stay local
*.md
!README.md
!LICENSE
!CLI_GUIDE.md
!ARCHITECTURE.md
!CONTRIBUTING.md
!TESTING.md
!RELEASE_NOTES.md
!FEATURE_SUMMARY.md
!.github/**/*.md

*.txt
# No .txt files are whitelisted - all are blocked
