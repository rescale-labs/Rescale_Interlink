module.exports = {
  root: true,
  env: { browser: true, es2020: true },
  extends: [
    'eslint:recommended',
    'plugin:@typescript-eslint/recommended',
    'plugin:react-hooks/recommended',
  ],
  parser: '@typescript-eslint/parser',
  parserOptions: { ecmaVersion: 'latest', sourceType: 'module' },
  plugins: ['react-refresh'],
  // wailsjs/ is generated by `wails generate module` and is overwritten on every
  // build, so lint fixes there would not survive.
  ignorePatterns: ['dist', 'wailsjs', '.eslintrc.cjs', 'coverage'],
  rules: {
    'react-refresh/only-export-components': [
      'warn',
      { allowConstantExport: true },
    ],
    // Underscore-prefixed bindings are the codebase's marker for a deliberately
    // unused parameter or an omitted destructured key.
    '@typescript-eslint/no-unused-vars': [
      'error',
      { argsIgnorePattern: '^_', varsIgnorePattern: '^_' },
    ],
    // Off, not warn: every current violation is a dependency array that was
    // trimmed on purpose, and "fixing" one changes when the effect re-runs.
    // Re-enable per-file once those effects have been audited individually.
    'react-hooks/exhaustive-deps': 'off',
  },
};
